name: 🚀 SERVER ngancute (Finalized)

on:
  workflow_dispatch:
    inputs:
      duration:
        description: 'Thời gian bên nhau (ngancute)'
        required: false
        default: '1_Giờ_30_Phút_(90m)'
        type: choice
        options:
          - '30_Phút_(30m)'
          - '1_Giờ_(60m)'
          - '1_Giờ_30_Phút_(90m)'
          - '2_Giờ_(120m)'
          - '3_Giờ_(180m)'
          - '4_Giờ_(240m)'
          - '5_Giờ_(300m)'
          - '6_Giờ_(360m)'

jobs:
  Ngancute-VPS:
    runs-on: windows-latest
    steps:
      # 1. KHỞI TẠO
      - name: KHỞI TẠO NGANCUTE
        shell: powershell
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          chcp 65001 > $null
          Write-Host "`n💖 SERVER NGANCUTE ĐANG KHỞI ĐỘNG..." -ForegroundColor Magenta

      # 2. TẠO USER NGANCUTE (Đã vá lỗi Password)
      - name: 👤 TẠO TÀI KHOẢN NGANCUTE
        shell: powershell
        run: |
          Write-Host "👤 Đang tạo user ngancute..." -ForegroundColor Yellow
          
          $pw = -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 12 | % {[char]$_})
          $securePass = ConvertTo-SecureString $pw -AsPlainText -Force
          
          Remove-LocalUser -Name "ngancute" -ErrorAction SilentlyContinue
          New-LocalUser -Name "ngancute" -Password $securePass -AccountNeverExpires -Description "Server Ngancute"
          
          Add-LocalGroupMember -Group "Administrators" -Member "ngancute"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "ngancute"
          
          echo "RDP_PASS=$pw" >> $env:GITHUB_ENV
          Write-Host "✅ User ngancute sẵn sàng." -ForegroundColor Green

      # 3. CÀI ĐẶT NGROK VÀ TẠO IP (Đã vá lỗi thời gian chờ)
      - name: 🌐 TẠO ĐỊA CHỈ IP (NGROK)
        env:
          NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
        run: |
          if (-not $env:NGROK_AUTH_TOKEN) { Write-Error "❌ Chưa nhập NGROK_AUTH_TOKEN vào Secrets!"; exit 1 }
          
          Invoke-WebRequest https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip -OutFile ngrok.zip
          Expand-Archive ngrok.zip
          .\ngrok\ngrok.exe config add-authtoken $env:NGROK_AUTH_TOKEN
          
          Start-Process -FilePath ".\ngrok\ngrok.exe" -ArgumentList "tcp 3389"
          
          Write-Host "⏳ Đang chờ Ngrok khởi động ổn định..."
          Start-Sleep -Seconds 15 # Đảm bảo Ngrok có đủ thời gian mở API 4040
          
          try {
              $tunnel = Invoke-RestMethod http://localhost:4040/api/tunnels
              $publicUrl = $tunnel.tunnels[0].public_url
              $address = $publicUrl -replace "tcp://", ""
              echo "SERVER_IP=$address" >> $env:GITHUB_ENV
              Write-Host "✅ Đã tạo IP thành công: $address" -ForegroundColor Green
          } catch {
              Write-Error "❌ Lỗi kết nối Ngrok. Kiểm tra lại Secret Key."
              exit 1
          }

      # 4. KÍCH HOẠT RDP
      - name: 🔓 MỞ KHÓA RDP
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Write-Host "✅ Đã mở cổng RDP." -ForegroundColor Green

      # 5. HIỂN THỊ THÔNG TIN KẾT NỐI
      - name: 🎉 THÔNG TIN KẾT NỐI
        run: |
          $durationInput = "${{ github.event.inputs.duration }}"
          if ($durationInput -match '\((\d+)m\)') { $minutes = [int]$matches[1] } else { $minutes = 60 }
          echo "MINUTES=$minutes" >> $env:GITHUB_ENV
          
          Write-Host "┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓" -ForegroundColor Magenta
          Write-Host "┃          💖 SERVER NGANCUTE IS READY      ┃" -ForegroundColor Magenta
          Write-Host "┣━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┫" -ForegroundColor Magenta
          Write-Host "┃ 🔗 IP Address:  $env:SERVER_IP" -ForegroundColor White
          Write-Host "┃ 👤 Username:    ngancute" -ForegroundColor White
          Write-Host "┃ 🔐 Password:    $env:RDP_PASS" -ForegroundColor White
          Write-Host "┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛" -ForegroundColor Magenta

      # 6. TREO MÁY (KEEP ALIVE)
      - name: ⏳ DUY TRÌ SERVER
        run: |
          $seconds = [int]$env:MINUTES * 60
          $end = (Get-Date).AddSeconds($seconds)
          
          while ((Get-Date) -lt $end) {
              $remaining = New-TimeSpan -Start (Get-Date) -End $end
              Write-Host "`r💓 Ngancute còn sống: $($remaining.Hours):$($remaining.Minutes):$($remaining.Seconds) " -NoNewline -ForegroundColor Magenta
              Start-Sleep -Seconds 5
          }
          
